
<% if current_user %>
  <div class="img-responsive center-block">
    <!--
    <div class="card text-center mx-auto" style="width: 12rem;">
      <% if current_user.avatar.present? %>  
        <%= image_tag current_user.avatar, width: "50", class: "mx-auto d-block card-img-top" %>
      <% else %>
        <h1 class="text-center my-5"> No Avatar </h1>
      <% end %>
      <h3 class="card-subtitle my-3"><%= fa_icon 'money 1x' %> <%= Plan.find(current_user.plan_id).name %></h3>
    </div>
    <h5 class="text-center mt-1 mb-0 text-white"><%= current_user.email %></h5>
    -->

    <div class="mt-5">
      <%= render 'projects/list' %>
    </div>

    <%= render 'shared/welcome_gallery' %>

    <div class="mx-auto d-block">
      <%= render 'shared/gallery' %>
    </div>
    
  </div>
<% else %>
  <%= render 'shared/pitch' %>
<% end %>
